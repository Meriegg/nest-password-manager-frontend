import { createStyles } from '@mantine/core'

export const useStyles = createStyles(() => ({
  mainContainer: {
    maxWidth: '550px',
    margin: 'auto',
  },
  form: {
    display: 'flex',
    flexDirection: 'column',
    gap: '15px',
  },
}))
