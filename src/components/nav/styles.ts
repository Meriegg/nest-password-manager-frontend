import { createStyles } from '@mantine/core'

export const useStyles = createStyles(() => ({
  main: {
    position: 'fixed',
    right: '30px',
    bottom: '30px',
    display: 'flex',
    flexWrap: 'wrap',
    gap: '10px',
    alignItems: 'center',
  },
}))
